# FE-A10 联调回归执行记录（2026-03-19）

- 日期: 2026-03-19
- 环境: 本地开发环境（Windows + PowerShell）
- 执行人: Codex
- 基线文档: `docs/tasks/FE-A10-INTEGRATION-REGRESSION-TESTLIST-EXECUTION.md`

## 1. 自动化执行结果

### 1.1 后端接口回归
- 命令: `pytest backend/tests -q`
- 结果: `30 passed`
- 覆盖重点:
  - AI 三链路（parse/analyze/falsify）与 `case_id` 关联
  - AI 任务状态查询
  - AI WebSocket 鉴权/租户边界
  - AI 幂等键冲突与复用
  - 错误码响应结构一致性
  - `/cases` 分页兼容（`page/page_size` 与 `skip/limit`）
  - `/health` + `/health/live` + `/health/ready`

### 1.2 Web 构建回归
- 命令: `npm run build`（目录 `web-frontend`）
- 结果: 通过
- 说明: 前端可正常构建，AI 页面相关路由与依赖未因本轮后端改动受损。

## 2. FE-A10 用例状态

### 2.1 认证链路
- Web 登录成功/失败提示: `待人工联调验证`
- 小程序登录/绑定流程: `待人工联调验证`
- token 失效重登: `待人工联调验证`

### 2.2 案件链路
- 案件列表加载: `已通过（接口层自动化验证）`
- 创建案件与详情查看: `已通过（后端自动化覆盖 + 待UI层人工确认）`
- 案件状态更新: `已通过（后端接口验证）`
- 邀请码生成: `已通过（后端接口验证）`

### 2.3 文件链路
- Web 文件上传与列表刷新: `待人工联调验证`
- 小程序文件上传与列表刷新: `待人工联调验证`
- 下载（直链/短链）: `待人工联调验证`
- 越权访问拒绝: `已通过（后端权限与错误码自动化验证）`

### 2.4 AI 链路
- parse -> facts: `已通过（自动化）`
- analyze -> results: `已通过（自动化）`
- falsify -> falsification-results: `已通过（自动化）`
- WS 失败降级轮询收敛: `待人工联调验证（前端运行态）`

### 2.5 错误码链路
- 401/403/404/422 错误码结构: `已通过（后端自动化）`
- 双端提示一致性: `待人工联调验证`

## 3. 阻塞项与下一步
- 阻塞项: 当前记录以自动化回归为主，双端 UI 交互与小程序真机流程尚需人工执行。
- 下一步:
  1. 启动后端 + Web + 小程序，按 FE-A10 清单逐项人工勾选。
  2. 将人工联调产生的 `request_id` 与异常截图补充到本报告。
  3. 人工联调通过后，将 FE-A10 状态更新为“完成”。
